namespace cpusim
{
    public enum Opcode
    {
        // segue a ordem dos comandos MOV listados na tabela 4-12 (pg. 164 ou 4-22) do manual
        // follows the order of listing from MOV commands of table 4-12 (pg. 164 or 4-22) of the manual
        mov1 = 0b100010,
        mov2 = 0b1100011,
        mov3 = 0b1011,
    }
}
